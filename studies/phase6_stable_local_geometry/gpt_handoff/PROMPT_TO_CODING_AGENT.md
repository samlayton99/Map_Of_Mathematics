# Prompt to the coding agent — Stable Local Geometry and Multiscale Atlas

Read the original core handoff again, then read every file in this package.

The main judgment is:

> Do not begin another broad score-combination search. First test whether the current score is compensating for a representational error: flattening the exact proof-term hierarchy into a bag of direct citations.

## Governing correction

The local canonical structure of an unchanged proof must be invariant under unrelated library growth.

Global use, rarity, centrality, communities, and learned routing may evolve as versioned sidecars.

Do not interpret this as banning every changing global quantity.

## Immediate work

### 1. Correct the substrate

- exact SCC depth;
- pure interface/statement closure;
- stable IDs;
- expression paths;
- application-parent relations;
- generated-owner relations;
- all occurrence roles;
- separate theorem proof bodies from definition unfolding.

### 2. Build a 48-proof typed hierarchy pilot

Compare:

- flat candidates;
- owner collapse;
- named-application hierarchy;
- hierarchy plus move/concept/infrastructure lanes.

No new fitted score.

### 3. Build the metamorphic benchmark

Use the transformations in `06_METAMORPHIC_VALIDATION.md`.

This is the primary independent test of machinery resistance.

### 4. Reframe `delta_depth`

Rename it abstraction span.

Test:

- absolute span;
- relative span;
- cited depth plus span;
- within-proof span rank;
- role plus span;
- contextual interface anchoring.

Do not use delta alone as a universality or junk classifier.

### 5. Separate map coordinates

Construct a multifiltration over:

- local salience/hierarchy;
- abstraction span;
- relation lane.

Do not collapse these axes into one weighted score.

### 6. Retain rarity appropriately

- live rarity remains a dynamic global field and upper-bound comparator;
- it may not define canonical local edges;
- test whether hierarchy reduces its apparent tail advantage;
- optionally distill it into a stable local student.

### 7. Independent validation

Prioritize:

- metamorphic invariance;
- exact expansion/reconstruction;
- artificial-jump rate;
- navigation tasks.

Use module co-location and held-out citation prediction only as secondary proxies.

## Required falsifiable hypotheses

Register H1–H10 from `04_HYPOTHESES.md`.

The highest-value result is H1:

> Does restoring local hierarchy reduce artificial shortcuts and ranking noise at equal recall?

## Do not

- build another V8-style weighted formula;
- call delta-depth universality;
- delete long-span edges;
- use shortest path as the only reading model;
- validate mainly with a junk mask built from the same features;
- conflate definition mentions with definition actions;
- postpone typed local moves again.

## Deliverables

1. `SUBSTRATE_CORRECTIONS.md`
2. `HIERARCHY_SCHEMA.md`
3. `METAMORPHIC_BENCHMARK.md`
4. `HIERARCHY_VS_FLAT_RESULTS.md`
5. `EDGE_SCALE_AND_SELF_SIMILARITY.md`
6. `MULTIFILTRATION_REPORT.md`
7. `INDEPENDENT_NAVIGATION_VALIDATION.md`
8. machine-readable occurrence/hierarchy data
9. updated `NEXT_RECOMMENDATION`

Preserve all historical reports unchanged.
