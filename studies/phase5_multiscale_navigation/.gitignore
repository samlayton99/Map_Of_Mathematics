# Derived incidence arrays: regenerable from the dump by src/build_incidence.py
# (18.7M incidences, ~134MB). Builders and reports are versioned; data is not.
data/*.npz
data/names.json

# Packaged dashboard: regenerable from dashboard/data by src/package_dashboard.py.
# The sources (viewer_head/body, charts.js, report.js) and the exported JSON
# payload ARE versioned; the assembled bundle is not.
dashboard/dist/
dashboard/data/all.js
