Compilers are basically a program that takes code, and turns it into a lower level representation. This will mark some programs as valid or not valid. 

Compiling programs often takes time. But once it is compiled, it can be ran many times. Interpreted languages compile every single time as it goes line by line. 